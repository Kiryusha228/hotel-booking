## Основная часть

Разработка началась с анализа требований и настройки миграций.
Как идентефикатор решил использовать UUID, т.к. он был использован шаблоне. Для связи таблиц booking и guest было решено использовать вспомогательную таблицу booking_guest со связями. Вариант создать поле booking в таблице guest не подошел т.к. гость может делать несколько бронирований в разные промежутки времени.

1. Были созданы 2 варианта моделей для работы с данными Entity, включающие в себя id для использования в коде, и dto без идентефикатора для использования в контроллерах. Был сделан dto для вывода исключений. Более узких dto не создавалось, т.к. задачи вывести специфические данные не было.

2. Для DAO слоя использовался NamedParameterJdbcTemplate. Были реализованы запросы по заданию и запрос на проверку существования для каждой таблицы для последующих проверок. Согласно заданию Были использованы нативные SQL запросы.

3. В сервисном слое реализованы методы по заданию, в проект был добавлены кастомные исключения и их обработчик, на уровне сервисного слоя была добавлена валидация и выброс пользовательских исключений.

4. В слое контроллеров возвращаемыми значениями решил сделать Entity

На выполнение основного задание было затрачено ~8 часов, половина из которых анализ требований и написание документации